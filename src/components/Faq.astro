---
import type { SourceLocale as Locale } from '@/i18n/config';
import Icon from '@/components/Icon.astro';

const { locale, items, title, topic } = Astro.props as {
  locale: Locale;
  /** what the FAQ is about (show or page name) — keeps the footer line unique per page */
  topic?: string;
  title?: string;
  items: { q: string; a: string }[];
};
---
<section class="sec sec--paper" id="faq">
  <div class="wrap wrap--narrow">
    <p class="kicker">{locale === 'ru' ? 'Частые вопросы' : 'Frequently asked'}</p>
    <h2 style="margin-bottom:1.4rem">{title ?? (locale === 'ru' ? 'Что спрашивают перед участием' : 'What people ask before joining')}</h2>
    <div class="acc">
      {
        items.map((item, i) => (
          <details open={i === 0}>
            <summary>
              <span>{item.q}</span>
            </summary>
            <div class="acc__body">{item.a}</div>
          </details>
        ))
      }
    </div>
    <p class="small muted" style="margin-top:1.2rem" data-nosnippet>
      <Icon name="telegram" size={15} /> {locale === 'ru'
        ? topic ? `Остался вопрос о ${topic}? Напишите менеджеру — ответим в рабочее время за 15 минут.` : 'Нет ответа на ваш вопрос? Напишите менеджеру — отвечаем в рабочее время за 15 минут.'
        : topic ? `Still have a question about ${topic}? Message the manager — we reply within 15 minutes during office hours.` : 'Missing an answer? Message the manager — we reply within 15 minutes during office hours.'}
    </p>
  </div>
</section>
